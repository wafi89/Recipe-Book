<?xml version="1.0" encoding="utf-8"?>
<resources>


    <style name="Platform.Theme.Recipe" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/status_bar_color</item>
    </style>

    <style name="Theme.RecipeSplash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/black</item>
        <item name="postSplashScreenTheme">@style/Theme.Recipe</item>
        <item name="windowSplashScreenAnimationDuration">2000</item>
    </style>

    <!-- The actual theme we use. This varies for light theme (here),
         and values-night for dark theme. -->
    <style name="Theme.Recipe" parent="Platform.Theme.Recipe" />

</resources>